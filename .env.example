DATABASE_URL=postgresql://schedule:schedule@localhost:5432/schedule_dev
LANYARD_URL=http://localhost:3000

# Create the service in the lanyard admin UI (Services → Create Service),
# then copy the key here.
LANYARD_SERVICE_KEY=

PORT=5050
